{
    "name": "meal-prep-app",
    "description": "the meal prep app",
    "dependencies": {
        "-": "^0.0.1",
        "firebase": "^9.4.1",
        "firebase-tools": "^9.6.1",
        "g": "^2.0.1",
        "standard": "*",
        "json-ptr": ">=3.0.0",
        "json-schema": ">=0.4.0"
    },
    "scripts": {
        "styling": "standard --fix"
    }
}
